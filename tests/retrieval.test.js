import test from 'node:test';import assert from 'node:assert/strict';const words=s=>new Set(s.toLowerCase().split(/\W+/).filter(w=>w.length>2));const score=(q,t)=>[...words(q)].filter(w=>words(t).has(w)).length;test('tenant query prefers tenant evidence',()=>assert.ok(score('tenant isolation','Every request carries a tenant context and PostgreSQL row-level security')>score('tenant isolation','Documents are split into stable chunks')));test('irrelevant query has zero overlap',()=>assert.equal(score('banana','PostgreSQL tenant context'),0));