export type DocumentChunk={id:string,title:string,text:string,tags:string[]}; export const knowledgeBase:DocumentChunk[]=[{id:'arch-01',title:'Tenant isolation',text:'Every request carries a tenant context. Application services resolve data through tenant-aware repositories. PostgreSQL row-level security provides a second enforcement boundary using tenant_id.',tags:['tenant','security','database']},{id:'arch-02',title:'Retrieval pipeline',text:'Documents are split into stable chunks. A retriever scores chunks by normalized keyword overlap, then a context builder keeps the highest scoring passages and their source metadata.',tags:['retrieval','ranking','context']},{id:'arch-03',title:'Answer provider',text:'The answer provider is an interface. The reference implementation can return a grounded deterministic answer; an LLM adapter can be added without changing retrieval or domain code.',tags:['llm','provider','architecture']},{id:'arch-04',title:'Observability',text:'Each retrieval records the query, candidate scores, selected chunk identifiers and answer sources. This trace makes relevance decisions inspectable and testable.',tags:['trace','observability','testing']}];